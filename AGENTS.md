# Payback PY workspace

For maintenance, use `.agents/skills/payback-operations/SKILL.md`.
Start with the bounded diagnostic `python scripts/audit_catalog.py`; avoid loading entire catalogs.
The consumer app is `app-web/`; the separate notification/account backend is `server/`.
Changes to both require separate GitHub Pages and Sites deployments.
